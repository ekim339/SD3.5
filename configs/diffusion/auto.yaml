# Selects the scheduler that belongs to the chosen network.
name: auto
sd3.5:
  name: flow_matching
  num_inference_steps: 40
  guidance_scale: 5.0
  prediction_type: flow
textctrl:
  name: pndm
  num_inference_steps: 50
  guidance_scale: 2.0
  prediction_type: epsilon
  skip_prk_steps: true
